name: Build Scamsung Kernel

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:
    inputs:
      kernel_repo:
        description: 'Kernel Repository URL'
        required: true
        default: ''
        type: string
      kernel_branch:
        description: 'Git branch'
        required: false
        default: ''
        type: string
      clang_version:
        description: 'Pilih Versi Clang Compiler'
        required: true
        default: 'clang-10'
        type: choice
        options:
          - 'clang-10'
          - 'proton-clang'
          - 'azure-clang'
      defconfig_name:
        description: 'Nama Defconfig (e.g. vendor/a23_eur_open_defconfig)'
        required: true
        default: 'vendor/a23_eur_open_defconfig'
        type: string

jobs:
  build-kernel:
    runs-on: ubuntu-latest

    steps:
    # 1. Mengkloning Repositori Utama Kamu (building-kernel)
    - name: Checkout WorkSpace
      uses: actions/checkout@v4

    # 2. Mengkloning Repositori Source Kernel ke folder terpisah
    - name: Download Kernel Source
      run: |
        if [ -z "${{ github.event.inputs.kernel_branch }}" ]; then
          echo "Cloning default branch dari ${{ github.event.inputs.kernel_repo }}..."
          git clone --depth=1 --recursive ${{ github.event.inputs.kernel_repo }} $GITHUB_WORKSPACE/kernel_src
        else
          echo "Cloning branch ${{ github.event.inputs.kernel_branch }} dari ${{ github.event.inputs.kernel_repo }}..."
          git clone --depth=1 -b ${{ github.event.inputs.kernel_branch }} --recursive ${{ github.event.inputs.kernel_repo }} $GITHUB_WORKSPACE/kernel_src
        fi

    # 3. Install Dependencies Sistem Ubuntu
    - name: Install Build Dependencies
      run: |
        sudo apt update -y
        sudo apt install aptitude zip ccache -y
        sudo apt install bc cpio flex bison git python-is-python3 libc6 libc6-dev tar perl curl lz4 -y
        sudo aptitude install libssl-dev -y
        curl https://rclone.org/install.sh | sudo bash

    # 4. Download Toolchains (GCC & Clang Berdasarkan Pilihan)
    - name: Download Toolchains
      run: |
        mkdir -p $GITHUB_WORKSPACE/toolchains
        cd $GITHUB_WORKSPACE/toolchains
        
        CHOSEN_CLANG="${{ github.event.inputs.clang_version }}"
        echo "Versi Clang yang dipilih: $CHOSEN_CLANG"
        
        if [ "$CHOSEN_CLANG" = "clang-10" ]; then
          echo "Downloading Clang 10..."
          git clone --depth=1 -b clang-10 https://github.com/rsuplaygrnd/toolchains.git clang
        elif [ "$CHOSEN_CLANG" = "proton-clang" ]; then
          echo "Downloading Proton Clang (Latest)..."
          git clone --depth=1 https://github.com/kdrag0n/proton-clang.git clang
        elif [ "$CHOSEN_CLANG" = "azure-clang" ]; then
          echo "Downloading Azure Clang..."
          git clone --depth=1 https://github.com/Padd3X/Azure-Clang.git clang
        fi
        
        echo "Downloading GCC..."
        git clone --depth=1 -b gcc https://github.com/blxyzY/toolchain.git gcc

    # 5. Jalankan Proses Kompilasi Kernel
    - name: Compile Kernel
      run: |
        export PATH="$GITHUB_WORKSPACE/toolchains/clang/bin:$GITHUB_WORKSPACE/toolchains/gcc/bin:$PATH"
        
        export ARCH=arm64
        export SUBARCH=arm64
        export OUT_DIR=$GITHUB_WORKSPACE/out
        
        export GCC_64=aarch64-linux-android-
        export GCC_32=arm-linux-androideabi-
        
        export USE_CCACHE=1
        export CCACHE_DIR=~/.cache/ccache
        ccache -M 5G
        
        mkdir -p $OUT_DIR
        
        cd $GITHUB_WORKSPACE/kernel_src
        
        DEFCONFIG="${{ github.event.inputs.defconfig_name }}"
        echo "Menggunakan Defconfig: $DEFCONFIG"
        
        make -j$(nproc --all) O=$OUT_DIR ARCH=arm64 CROSS_COMPILE=$GCC_64 CROSS_COMPILE_ARM32=$GCC_32 CC=clang CLANG_TRIPLE=aarch64-linux-gnu- $DEFCONFIG
        make -j$(nproc --all) O=$OUT_DIR ARCH=arm64 CROSS_COMPILE=$GCC_64 CROSS_COMPILE_ARM32=$GCC_32 CC=clang CLANG_TRIPLE=aarch64-linux-gnu- CONFIG_SECTION_MISMATCH_WARN_ONLY=y CONFIG_DEBUG_SECTION_MISMATCH=y

    # 6. Mengemas Kernel ke dalam Folder Kerja Bersih (Anti-Dobel Zip)
    - name: Package Kernel
      run: |
        echo "Membersihkan dan mempersiapkan kemasan zip baru..."
        
        rm -rf $GITHUB_WORKSPACE/zip_stage
        mkdir -p $GITHUB_WORKSPACE/zip_stage
        
        if [ -d "$GITHUB_WORKSPACE/ScamsungKernel" ]; then
          cp -r $GITHUB_WORKSPACE/ScamsungKernel/* $GITHUB_WORKSPACE/zip_stage/
          rm -f $GITHUB_WORKSPACE/zip_stage/*.zip
        else
          echo "ERROR: Folder ScamsungKernel tidak ditemukan!"
          exit 1
        fi
        
        if [ -f "$GITHUB_WORKSPACE/out/arch/arm64/boot/Image" ]; then
          cp $GITHUB_WORKSPACE/out/arch/arm64/boot/Image $GITHUB_WORKSPACE/zip_stage/Image
          echo "Berhasil memasukkan berkas: Image"
        else
          echo "ERROR: Berkas Image tidak ditemukan!"
          exit 1
        fi
        
        if [ -f "$GITHUB_WORKSPACE/out/arch/arm64/boot/dtbo.img" ]; then
          cp $GITHUB_WORKSPACE/out/arch/arm64/boot/dtbo.img $GITHUB_WORKSPACE/zip_stage/dtbo.img
          echo "Berhasil memasukkan berkas: dtbo.img"
        fi
        
        cd $GITHUB_WORKSPACE/zip_stage
        zip -r9 ../ScamsungKernel.zip *
        echo "Pembuatan zip tunggal selesai."

    # 7. Konfigurasi Rclone (DIPERBAIKI PADA FORMAT INPUT EXPIRY)
    - name: Upload to Google Drive
      run: |
        echo "Mengonfigurasi koneksi Google Drive..."
        mkdir -p ~/.config/rclone
        
        cat <<EOF > ~/.config/rclone/rclone.conf
        [gdrive]
        type = drive
        client_id = ${{ secrets.GDRIVE_CLIENT_ID }}
        client_secret = ${{ secrets.GDRIVE_CLIENT_SECRET }}
        token = {"access_token":"","token_type":"Bearer","refresh_token":"${{ secrets.GDRIVE_REFRESH_TOKEN }}","expiry":"2006-01-02T15:04:05Z"}
        EOF
        
        echo "Memulai proses unggah berkas ke Google Drive..."
        if [ -f "$GITHUB_WORKSPACE/ScamsungKernel.zip" ]; then
          rclone copy $GITHUB_WORKSPACE/ScamsungKernel.zip gdrive:${{ secrets.GDRIVE_FOLDER_ID }} --progress
          echo "Berhasil Terunggah ke Google Drive!"
        else
          echo "ERROR: File zip tidak ditemukan untuk diunggah!"
          exit 1
        fi

    # 8. Sediakan Cadangan Di Halaman Artifact GitHub
    - name: Upload Kernel Artifact
      uses: actions/upload-artifact@v4
      with:
        name: ScamsungKernel-Zip-Package
        path: ScamsungKernel.zip
